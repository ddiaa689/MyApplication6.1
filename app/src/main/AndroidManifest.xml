<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="My Application"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".JobListActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".JobDetailActivity" />
        <activity android:name=".MessageDetailActivity" />
        <activity android:name=".RecruiterMainActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".LoginActivity" android:exported="true" />
        <!-- 启动页是 JobSeekerMainActivity -->
        <activity android:name=".JobSeekerMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>